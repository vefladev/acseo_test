{# bouton permettant le traitement du message #}
<form method="post" action="{{ path('message_traitement', {'id': message.id}) }}" onsubmit="return confirm('Avez vous bien traité cette demande ?');">
	<button class="btn btn-primary">Définir comme traitée</button>
</form>
